# BackRoute Server

The server accepts agent connections, tracks online/offline state, and serves the starter dashboard.

## Run Locally

```bash
go run .
```

Environment variables:

```text
BACKROUTE_ADDR=:8080
BACKROUTE_TOKEN=dev-token
```
